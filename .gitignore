assets/
node_modules
dist
.env
*.local
.artifacts
.artifacts-test

# Docker orchestration — real env files (only *.example are committed)
.env.demo
.env.local
.env.vps
# infra/scripts/unit-deploy.sh writes .env.vps atomically via mktemp .env.vps.XXXXXX —
# those temp copies hold the same secrets and must never be committed if one is orphaned.
.env.vps.??????

# Git worktrees (managed by tooling)
.worktrees/

# Superpowers visual companion runtime state (keep brainstorm content/*.html)
/.superpowers/brainstorm/*/state/

# Eval run outputs (generated artifacts)
docs/eval-outputs/

# subagent-driven scratch
.superpowers/

# dev orchestration env (generated from .env.dev.example)
.env.dev

# Vim swap files
*.swp
.env.swp

# gortex daemon volatile sidecar (WAL/SHM) — never commit
.gortex/sidecar.sqlite-shm
.gortex/sidecar.sqlite-wal
